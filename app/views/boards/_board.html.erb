<div id="<%= dom_id board %>" class="flex items-center justify-between gap-4 border border-neutral-400 shadow-md rounded-md p-4">
  <div class="flex gap-4">
    <%= board.title %>
  </div>
  <div class="flex gap-4">
    <%= link_to "Edytuj", edit_board_path(board), class: "btn" %>
    <%= button_to "Usuń", board, method: :delete, class: "btn btn-error", data: { turbo_confirm: "Are you sure?" } %>
  </div>
</div>

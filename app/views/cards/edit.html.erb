<turbo-frame id="modal">
  <div class="modal-overlay py-12 fixed backdrop-blur-sm w-full h-full z-99999999 flex justify-center items-start overflow-y-scroll" data-controller="modal">
    <div class="modal-content">
      <%= render "form", board: @board, list: @list, card: @card %>
    </div>
  </div>
</turbo-frame>
<%= turbo_stream.prepend "comments" do %>
  <%= render partial: "comment", locals: {comment: @comment} %>
<% end %>
<%= turbo_stream.replace "new_comment",
    partial: "form",
    locals: {board: @board, list: @list, card: @card, comment: Comment.new}
%>